# 사내 파일검색 챗봇 개발기획서

## 1. 프로젝트 개요

### 1.1 프로젝트명
사내 파일검색 챗봇 구축

### 1.2 개발 목적
사내 서버 및 공유폴더에 저장된 문서를 사용자가 자연어로 검색하고, 필요한 파일의 위치를 빠르게 확인할 수 있는 사내 챗봇을 구축한다.

현재 사내 자료는 여러 폴더 및 하위 폴더에 분산되어 있어 사용자가 파일명이나 저장 위치를 정확히 기억하지 못하면 자료 탐색에 시간이 소요된다.

본 프로젝트는 사용자가

> “스타필드 제안서 찾아줘”

와 같이 일반적인 문장으로 요청하면 관련 파일을 검색하여 파일명과 저장 경로를 제시하는 기능을 1차 목표로 한다.

---

## 2. 개발 목표

### 2.1 핵심 목표

사용자의 자연어 요청을 기반으로 사내 파일을 검색하고 가장 관련성이 높은 파일을 빠르게 제공한다.

### 2.2 세부 목표

- 사내 지정 폴더 및 하위 폴더 파일 검색
- 파일명 기반 검색
- 폴더명 기반 검색
- 검색 결과 관련도 순 정렬
- 파일 전체 경로 제공
- 파일 확장자 표시
- 파일 수정일 표시
- 검색 결과 다중 제공
- 사용자 친화적인 챗봇 형태의 검색 UI 제공

### 2.3 1차 MVP 성공 기준

사용자가 찾으려는 파일의 이름을 정확히 기억하지 못하더라도 일부 키워드만 입력하여 관련 파일을 찾을 수 있어야 한다.

예시:

사용자 입력:

`스타필드 광고 제안서 찾아줘`

검색 결과:

`스타필드_광고운영_제안서_최종.pptx`

경로:

`\\회사NAS\제안서\2026\스타필드\스타필드_광고운영_제안서_최종.pptx`

---

## 3. 개발 범위

### 3.1 1차 MVP 포함 기능

| 기능 | 적용 여부 | 내용 |
|---|---|---|
| 자연어 검색창 | 적용 | 사용자가 일반 문장으로 검색 |
| 파일명 검색 | 적용 | 파일명에 포함된 키워드 검색 |
| 폴더명 검색 | 적용 | 폴더명에 포함된 키워드 검색 |
| 하위폴더 검색 | 적용 | 지정 경로 전체 검색 |
| 복수 검색 결과 | 적용 | 관련 파일 여러 개 출력 |
| 파일 경로 제공 | 적용 | 파일 전체 저장경로 표시 |
| 확장자 표시 | 적용 | PPTX, PDF, XLSX 등 |
| 수정일 표시 | 적용 | 최근 문서 판단 지원 |
| 파일 크기 표시 | 선택 | 검색 결과 참고 정보 |
| 파일 열기 | 선택 | 사용자 PC 환경 검토 후 적용 |
| 폴더 열기 | 선택 | 사용자 PC 환경 검토 후 적용 |

### 3.2 1차 MVP 제외 기능

다음 기능은 향후 확장 기능으로 분리한다.

| 기능 | 단계 |
|---|---|
| PDF/PPT/DOCX 내부 내용 검색 | 2차 |
| 문서 내용 미리보기 | 2차 |
| 문서 자동 요약 | 2~3차 |
| LLM 기반 의미 검색 | 2~3차 |
| Vector DB | 2~3차 |
| RAG | 3차 |
| 문서 내용 질문·답변 | 3차 |
| 제안서 자동 생성 | 4차 |
| 사용자별 파일 접근권한 | 별도 검토 |
| 문서 버전 관리 | 별도 검토 |

---

## 4. 사용자 정의

### 4.1 주요 사용자

사내 임직원

### 4.2 사용 환경

- 사내 PC
- Windows 기반
- 사내 네트워크
- 웹 브라우저
- 사내 NAS 또는 파일서버 접근 가능 환경

### 4.3 대표 사용자 상황

#### 상황 A

사용자가 파일명을 일부 기억하는 경우

`미니쉬 제안서 찾아줘`

#### 상황 B

파일명은 모르지만 관련 프로젝트명을 기억하는 경우

`스타필드 관련 자료 찾아줘`

#### 상황 C

특정 파일 유형을 찾는 경우

`스타필드 엑셀 파일 찾아줘`

#### 상황 D

최근 파일을 원하는 경우

`최근에 수정한 스타필드 제안서 찾아줘`

---

## 5. 사용자 흐름

사용자의 기본 이용 흐름은 다음과 같다.

사용자 접속

→ 검색어 입력

→ 검색 요청 전송

→ 검색조건 분석

→ 파일 DB 검색

→ 관련도 계산

→ 검색 결과 반환

→ 사용자에게 파일명 및 경로 표시

→ 필요 시 파일 또는 폴더 열기

---

## 6. 시스템 구성

### 6.1 전체 구조

```text
[사내 파일서버 / NAS]
          ↓
   파일 인덱싱 프로그램
          ↓
     [파일정보 DB]
          ↓
      검색 Backend
          ↓
       REST API
          ↓
      Web Frontend
          ↓
        사용자
```

### 6.2 구성 요소

#### 파일서버 / NAS

실제 문서가 저장되는 장소.

예:

```text
\\NAS\공용자료
\\NAS\제안서
\\FILESERVER\영업자료
```

#### 파일 인덱싱 프로그램

지정된 폴더와 하위폴더를 탐색하여 파일 정보를 수집한다.

수집 대상:

- 파일명
- 전체 경로
- 폴더 경로
- 확장자
- 파일 크기
- 생성일
- 수정일

파일 자체를 DB에 저장하지 않는다.

#### 파일정보 DB

검색 속도를 높이기 위해 파일 메타데이터를 저장한다.

사용자가 검색할 때 실제 NAS 전체를 매번 탐색하지 않고 DB를 검색하는 방식으로 구성한다.

#### Backend

사용자 검색어를 전달받고 검색조건을 처리하여 DB에서 관련 파일을 검색한다.

#### Frontend

사용자가 검색어를 입력하고 검색 결과를 확인하는 웹 인터페이스.

---

## 7. 파일 인덱싱 방식

### 7.1 기본 방식

서버에서 지정된 경로를 주기적으로 탐색한다.

예:

```text
\\NAS\회사자료
```

하위폴더:

```text
\\NAS\회사자료\제안서
\\NAS\회사자료\보고서
\\NAS\회사자료\계약서
\\NAS\회사자료\마케팅
```

각 파일의 메타데이터를 DB에 저장한다.

### 7.2 최초 인덱싱

시스템 최초 실행 시 전체 폴더를 검색하여 파일 정보를 DB에 등록한다.

### 7.3 갱신 인덱싱

이후에는 일정 주기로 변경사항을 확인한다.

예:

```text
5분
10분
30분
1시간
```

구체적인 갱신 주기는 실제 사내 파일 수와 서버 성능을 확인한 후 결정한다.

### 7.4 파일 변경 처리

신규 파일

→ DB 신규 등록

수정 파일

→ 수정일 등 메타데이터 업데이트

삭제 파일

→ DB 삭제 또는 비활성 상태 처리

이동 파일

→ 기존 경로 변경 처리

---

## 8. 데이터베이스 설계

### 8.1 files 테이블

| 필드 | 내용 |
|---|---|
| id | 내부 고유번호 |
| file_name | 파일명 |
| extension | 파일 확장자 |
| full_path | 파일 전체 경로 |
| folder_path | 폴더 경로 |
| file_size | 파일 크기 |
| created_at | 파일 생성일 |
| modified_at | 파일 수정일 |
| indexed_at | 마지막 인덱싱 일시 |
| is_active | 현재 파일 존재 여부 |

예시 데이터:

```text
id
1024

file_name
스타필드_광고운영_제안서_최종.pptx

extension
pptx

full_path
\\NAS\제안서\2026\스타필드\스타필드_광고운영_제안서_최종.pptx

folder_path
\\NAS\제안서\2026\스타필드

modified_at
2026-08-20
```

---

## 9. 검색 기능 설계

### 9.1 1차 검색 대상

검색 대상은 다음 항목으로 제한한다.

- 파일명
- 폴더명
- 확장자
- 수정일

### 9.2 검색어 처리

사용자 입력:

`스타필드 광고 제안서 찾아줘`

검색에 불필요한 표현:

`찾아줘`

핵심 검색어:

`스타필드`
`광고`
`제안서`

검색엔진에서는 핵심 검색어가 포함된 파일 및 폴더를 검색한다.

### 9.3 검색 관련도

관련도가 높은 결과를 위쪽에 노출한다.

예시 기준:

파일명 완전 일치

>

파일명에 모든 키워드 포함

>

파일명에 일부 키워드 포함

>

폴더명 키워드 포함

>

부분 문자열 일치

향후 사용 데이터를 확보한 후 검색 점수 방식을 개선한다.

---

## 10. 검색 예외 처리

### 검색 결과가 없는 경우

사용자:

`A프로젝트 보고서 찾아줘`

챗봇:

`검색 결과를 찾지 못했습니다.`

추가 안내:

`A프로젝트`

와 같이 검색어를 줄여서 다시 검색할 수 있도록 안내한다.

### 검색 결과가 너무 많은 경우

사용자:

`제안서 찾아줘`

결과가 수백 건 발생하면 최신 문서를 우선 제공하고 추가 검색조건을 제시한다.

예:

`관련 파일이 327개 있습니다.`

`프로젝트명이나 연도를 추가하면 더 정확하게 찾을 수 있습니다.`

---

## 11. API 구성

### 파일 검색

```text
GET /api/search
```

예:

```text
/api/search?q=스타필드+제안서
```

응답 예시:

```json
{
  "query": "스타필드 제안서",
  "count": 2,
  "results": [
    {
      "file_name": "스타필드_광고제안서_최종.pptx",
      "extension": "pptx",
      "path": "\\\\NAS\\제안서\\2026\\스타필드_광고제안서_최종.pptx",
      "modified_at": "2026-08-20"
    }
  ]
}
```

### 시스템 상태 확인

```text
GET /api/health
```

### 인덱스 상태 확인

향후 관리자용 기능으로 추가한다.

```text
GET /api/index/status
```

---

## 12. 기술 구성안

1차 MVP에서는 구현 난이도와 유지관리성을 우선한다.

### Backend

Python

FastAPI

### Database

초기 개발:

SQLite

실제 사내 운영 및 확장:

PostgreSQL 검토

### Frontend

React

TypeScript

Vite

### 파일 탐색

Python 표준 파일 시스템 라이브러리 활용

### 운영 환경

사내 서버 또는 사내 PC 서버

외부 Cloud를 사용하지 않는 Local / On-Premise 형태를 우선 검토한다.

---

## 13. 보안 기본 원칙

1차 MVP에서도 파일 검색 시스템인 만큼 보안 요소는 처음부터 고려한다.

파일 자체를 별도 Cloud에 업로드하지 않는다.

DB에는 파일 원본이 아닌 메타데이터만 저장한다.

검색 서버가 접근 가능한 사내 폴더만 검색한다.

사용자가 검색한다고 해서 원래 접근할 수 없는 파일에 접근할 수 있게 만들어서는 안 된다.

향후 실제 배포 전 다음 항목을 추가 검토한다.

- 사용자 인증
- AD/사내 계정 연동
- 사용자별 파일 접근 권한
- 검색 로그
- 파일 열람 로그
- 관리자 기능

---

## 14. AI/LLM 적용 방침

1차 MVP에서는 LLM을 필수 요소로 사용하지 않는다.

초기 검색 기능은 일반 검색 방식으로 구현한다.

이유:

- 개발 복잡도 최소화
- 비용 최소화
- 사내 문서 외부 전송 방지
- 검색 품질 기준 수립 용이
- 장애 원인 파악 용이

향후 자연어 검색 요구가 높아지면 LLM을 검색어 분석 용도로 제한적으로 적용할 수 있다.

예:

사용자:

`작년에 우리가 스타필드에 냈던 광고 관련 제안서를 찾아줘`

LLM 분석:

```text
기간 = 2025
프로젝트 = 스타필드
주제 = 광고
문서유형 = 제안서
```

이후 실제 검색은 내부 파일 DB가 수행한다.

---

## 15. 향후 확장 로드맵

### 1단계 — 파일 검색

`스타필드 제안서 찾아줘`

↓

관련 파일과 경로 제공

### 2단계 — 문서 내용 검색

`스타필드 방문객 숫자가 들어있는 자료 찾아줘`

↓

PDF / PPT / Word / Excel 내부 텍스트 검색

### 3단계 — 문서 질의응답

`이 제안서에서 스타필드 광고 전략이 뭐였어?`

↓

관련 문서 검색 + 내용 답변

### 4단계 — 문서 요약

`이 제안서 요약해줘`

↓

문서 내용을 기반으로 요약 생성

### 5단계 — 문서 생성

`기존 스타필드 제안서를 참고해서 새로운 제안서 초안을 만들어줘`

↓

기존 사내 자료 기반 신규 문서 생성

---

## 16. 1차 개발 완료 기준

다음 조건을 만족하면 MVP 개발이 완료된 것으로 판단한다.

- 지정된 사내 폴더 스캔 가능
- 파일정보 DB 저장 가능
- 신규/수정 파일 인덱스 갱신 가능
- 검색어 입력 가능
- 파일명 검색 가능
- 폴더명 검색 가능
- 관련 검색 결과 복수 제공
- 검색 결과 관련도 순 정렬
- 파일 전체 경로 표시
- 수정일 표시
- 실제 사내 테스트 데이터 검색 성공

---

## 17. 프로젝트 핵심 원칙

본 프로젝트는 처음부터 범용 AI 사내 챗봇을 구축하는 것을 목표로 하지 않는다.

1차 목표는 다음 한 가지에 집중한다.

> **“찾고 싶은 사내 파일을 빠르게 찾아주는 것.”**

파일 검색 기능을 안정적으로 구축한 이후 문서 내용검색, RAG, LLM, 문서요약 및 제안서 자동생성 기능으로 단계적으로 확장한다.
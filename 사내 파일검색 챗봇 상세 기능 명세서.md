# 사내 파일검색 챗봇 상세 기능 명세서

## 1. 문서 개요

### 1.1 문서 목적

본 문서는 사내 파일검색 챗봇 MVP 개발을 위한 상세 기능 명세를 정의한다.

개발자는 본 문서를 기준으로 다음 항목을 구현한다.

- 파일 시스템 스캔
- 파일 메타데이터 수집
- 파일 인덱스 DB 관리
- 검색어 처리
- 파일 검색
- 검색 결과 정렬
- API 제공
- Frontend 검색 UI
- 오류 처리
- 로그 관리

---

# 2. MVP 정의

## 2.1 핵심 사용자 기능

사용자는 웹 화면에서 자연어 또는 키워드 형태로 검색한다.

예:

`스타필드 제안서 찾아줘`

시스템은 등록된 사내 공유폴더 내 파일 정보를 검색하고 다음 정보를 제공한다.

- 파일명
- 파일 종류
- 파일 저장 경로
- 상위 폴더
- 수정일
- 파일 크기
- 검색 관련도

---

# 3. 전체 시스템 구조

```text
[사내 NAS / 파일서버]
        │
        │ 파일 탐색
        ▼
[Indexing Service]
        │
        │ 파일 메타데이터
        ▼
[Database]
        │
        │ 검색 Query
        ▼
[FastAPI Backend]
        │
        │ REST API
        ▼
[React Frontend]
        │
        ▼
[사내 사용자]
```

---

# 4. 시스템 구성요소

## 4.1 Frontend

권장 기술:

- React
- TypeScript
- Vite

역할:

- 검색어 입력
- 검색 API 호출
- 검색 결과 표시
- 오류 표시
- 검색 상태 표시

---

## 4.2 Backend

권장 기술:

- Python
- FastAPI

역할:

- 검색 요청 처리
- 검색어 정제
- DB 조회
- 검색 관련도 계산
- 검색 결과 정렬
- API 응답
- 시스템 상태 제공

---

## 4.3 Database

개발 단계:

SQLite

운영 확장 시:

PostgreSQL

주요 저장 데이터:

- 파일 메타데이터
- 인덱싱 상태
- 검색 로그

---

## 4.4 Indexing Service

역할:

사내 지정 폴더 및 하위 폴더를 탐색해 파일 메타데이터를 DB에 등록한다.

---

# 5. 주요 사용자 시나리오

## UC-001 파일 검색

사용자 입력:

`스타필드 제안서 찾아줘`

처리:

1. 검색어 수신
2. 불필요한 표현 제거
3. 핵심 검색어 추출
4. DB 검색
5. 관련도 계산
6. 결과 정렬
7. 결과 반환

출력 예:

```text
스타필드_광고제안서_최종.pptx

\\NAS\영업본부\2026\스타필드\스타필드_광고제안서_최종.pptx

수정일: 2026.08.20
```

---

# 6. 기능 요구사항

---

# FR-001 검색어 입력

## 기능명

검색어 입력

## 설명

사용자가 검색창에 자연어 또는 키워드를 입력할 수 있다.

## 입력 예

```text
스타필드
```

```text
스타필드 제안서
```

```text
스타필드 제안서 찾아줘
```

## 입력 조건

최소:

1자

권장 최대:

200자

## Frontend 검증

공백만 입력한 경우 검색 요청을 전송하지 않는다.

예:

```text
"     "
```

출력:

`검색어를 입력해주세요.`

## 완료 조건

- 검색어 입력 가능
- Enter 키 검색 가능
- 검색 버튼 클릭 가능
- 공백 검색 차단

---

# FR-002 검색어 전처리

## 기능명

검색어 정규화

## 목적

사용자의 자연어 표현에서 검색에 불필요한 단어를 제거한다.

## 입력

```text
스타필드 광고 제안서 좀 찾아줘
```

## 기본 전처리

앞뒤 공백 제거

```text
" 스타필드 제안서 "
```

↓

```text
"스타필드 제안서"
```

중복 공백 제거

```text
스타필드    제안서
```

↓

```text
스타필드 제안서
```

대소문자 통일

```text
STARFIELD
```

↓

```text
starfield
```

## 불용어 예시

```text
찾아줘
찾아
검색해줘
검색
보여줘
좀
파일
자료
있어
있는
관련
```

주의:

불용어는 별도 설정 파일로 관리한다.

예:

```text
config/stopwords.json
```

## 처리 예시

입력:

```text
스타필드 제안서 좀 찾아줘
```

처리 결과:

```json
{
  "original": "스타필드 제안서 좀 찾아줘",
  "keywords": [
    "스타필드",
    "제안서"
  ]
}
```

---

# FR-003 파일 인덱스 생성

## 기능명

파일 인덱싱

## 설명

등록된 검색 대상 폴더 전체를 탐색해 파일 정보를 DB에 저장한다.

## 검색 대상 설정 예

```text
\\NAS\공용자료
```

또는

```text
D:\company_data
```

## 하위 폴더

모든 하위 폴더를 재귀적으로 검색한다.

예:

```text
공용자료
 ├─ 영업
 │   └─ 제안서
 ├─ 마케팅
 └─ 운영
```

전체 검색 대상에 포함한다.

---

# FR-004 파일 메타데이터 수집

각 파일에 대해 다음 정보를 수집한다.

필수:

```text
file_name
extension
full_path
folder_path
file_size
created_at
modified_at
indexed_at
```

선택:

```text
parent_folder
depth
```

예:

```json
{
  "file_name": "스타필드_광고제안서.pptx",
  "extension": "pptx",
  "full_path": "\\\\NAS\\제안서\\2026\\스타필드_광고제안서.pptx",
  "folder_path": "\\\\NAS\\제안서\\2026",
  "file_size": 28301221,
  "created_at": "2026-07-01T10:11:23",
  "modified_at": "2026-08-20T14:22:11",
  "indexed_at": "2026-08-24T10:00:00"
}
```

---

# FR-005 파일 중복 처리

동일 경로의 파일은 하나의 파일로 처리한다.

기준:

```text
full_path
```

DB에는 unique constraint를 적용한다.

예:

```text
UNIQUE(full_path)
```

---

# FR-006 신규 파일 처리

인덱싱 실행 시 DB에 존재하지 않는 파일을 발견하면 신규 등록한다.

상태:

```text
is_active = true
```

---

# FR-007 파일 수정 처리

다음 값이 변경된 경우 파일 정보를 업데이트한다.

```text
modified_at
file_size
```

업데이트 항목:

```text
modified_at
file_size
indexed_at
```

---

# FR-008 파일 삭제 처리

DB에는 존재하지만 실제 파일이 존재하지 않는 경우:

```text
is_active = false
```

처리한다.

1차 MVP에서는 DB에서 즉시 삭제하지 않는다.

이유:

- 파일 이동 여부 확인
- 오류 추적
- 향후 로그 분석

---

# FR-009 파일 이동 처리

파일 이동은 MVP에서는 다음과 같이 처리한다.

기존 파일:

```text
is_active = false
```

신규 경로:

새로운 파일로 등록

향후 파일 Hash 등을 활용해 동일 파일 이동 여부를 판단할 수 있다.

---

# FR-010 파일 확장자 필터

기본 검색 대상 확장자:

```text
ppt
pptx
pdf
doc
docx
xls
xlsx
csv
txt
hwp
hwpx
jpg
jpeg
png
zip
```

확장자 목록은 설정 파일로 관리한다.

예:

```text
config/extensions.json
```

검색 제외 예:

```text
.tmp
.dll
.exe
```

---

# FR-011 파일명 검색

사용자가 입력한 키워드를 파일명과 비교한다.

예:

검색어:

```text
스타필드
```

검색 대상:

```text
스타필드_광고제안서.pptx
```

→ 검색 결과 포함

---

# FR-012 다중 키워드 검색

검색어:

```text
스타필드 광고 제안서
```

키워드:

```text
스타필드
광고
제안서
```

파일명:

```text
2026_스타필드_광고제안서.pptx
```

→ 높은 검색 점수

파일명:

```text
스타필드_운영보고서.pdf
```

→ 낮은 검색 점수

---

# FR-013 폴더명 검색

파일명에 검색어가 없더라도 상위 폴더 경로에 검색어가 포함되어 있다면 검색 결과에 포함한다.

예:

검색어:

```text
스타필드
```

파일:

```text
최종제안서.pptx
```

경로:

```text
\\NAS\영업\스타필드\최종제안서.pptx
```

→ 검색 결과 포함

---

# FR-014 검색 점수

1차 MVP에서는 간단한 점수 기반 검색을 사용한다.

예시 점수:

| 조건 | 점수 |
|---|---:|
| 파일명 전체 일치 | +100 |
| 파일명 검색어 완전 포함 | +70 |
| 파일명 모든 키워드 포함 | +60 |
| 파일명 키워드 1개 포함 | +20 |
| 폴더명 전체 검색어 포함 | +30 |
| 폴더명 키워드 포함 | +10 |
| 최근 수정 문서 | 보정 |

예:

검색:

```text
스타필드 제안서
```

파일 A:

```text
스타필드_제안서.pptx
```

점수:

```text
100
```

파일 B:

```text
2026_스타필드_광고_제안서.pptx
```

점수:

```text
60
```

파일 C:

```text
스타필드_회의자료.pptx
```

점수:

```text
20
```

---

# FR-015 최신 파일 가중치

동일한 검색 관련도를 가진 파일은 최근 수정 파일을 우선 표시한다.

정렬 순서:

```text
search_score DESC
modified_at DESC
```

---

# FR-016 검색 결과 개수

기본:

```text
20개
```

최대:

```text
100개
```

API 파라미터:

```text
limit
```

예:

```text
/api/search?q=스타필드&limit=20
```

---

# FR-017 검색 결과 반환

검색 결과에는 다음 정보를 반환한다.

```text
파일명
확장자
전체 경로
폴더 경로
파일 크기
생성일
수정일
검색 점수
```

---

# FR-018 검색 결과 없음

검색 결과:

```text
0건
```

인 경우

Frontend:

```text
검색 결과를 찾지 못했습니다.
```

추가 안내:

```text
검색어를 줄이거나 다른 키워드를 입력해보세요.
```

---

# FR-019 검색 결과 다수

결과가 기본 표시 수보다 많은 경우:

```text
관련 파일 327개 중 20개를 표시합니다.
```

표시한다.

---

# FR-020 확장자 검색

사용자 입력:

```text
스타필드 PPT 찾아줘
```

처리:

```text
keyword = 스타필드
extension = ppt,pptx
```

사용자 표현 매핑:

```text
PPT → ppt, pptx
엑셀 → xls, xlsx
Excel → xls, xlsx
워드 → doc, docx
PDF → pdf
한글 → hwp, hwpx
```

---

# FR-021 날짜 검색

2차 MVP 후보이나 기본 구조는 준비한다.

예:

```text
최근 스타필드 제안서
```

최근:

modified_at 기준 정렬

예:

```text
2025년 스타필드 제안서
```

→ modified_at 연도 필터

1차 MVP에서는 명시적인 날짜 필터 UI 없이 Backend에서 지원 가능하도록 설계한다.

---

# FR-022 검색 API

## Endpoint

```text
GET /api/search
```

## Parameter

```text
q
```

필수

```text
limit
```

선택

기본:

```text
20
```

---

## 요청 예

```text
GET /api/search?q=스타필드+제안서&limit=20
```

---

## 성공 Response

HTTP:

```text
200
```

```json
{
  "query": "스타필드 제안서",
  "keywords": [
    "스타필드",
    "제안서"
  ],
  "total": 12,
  "count": 12,
  "results": [
    {
      "id": 1024,
      "file_name": "스타필드_광고제안서_최종.pptx",
      "extension": "pptx",
      "full_path": "\\\\NAS\\영업\\제안서\\스타필드_광고제안서_최종.pptx",
      "folder_path": "\\\\NAS\\영업\\제안서",
      "file_size": 28301221,
      "modified_at": "2026-08-20T14:22:11",
      "score": 90
    }
  ]
}
```

---

# FR-023 검색어 오류

검색어가 없을 경우:

```text
GET /api/search?q=
```

Response:

HTTP:

```text
400
```

```json
{
  "detail": "검색어를 입력해주세요."
}
```

---

# FR-024 Health Check

Endpoint:

```text
GET /api/health
```

응답:

```json
{
  "status": "ok"
}
```

DB 상태 포함 권장:

```json
{
  "status": "ok",
  "database": "connected"
}
```

---

# FR-025 인덱스 실행 API

관리자 또는 개발 환경에서 수동 인덱싱을 실행할 수 있도록 한다.

Endpoint:

```text
POST /api/admin/index
```

1차 개발에서는 인증 없이 개발환경에서만 사용하고 운영 배포 시 관리자 인증을 적용한다.

응답:

```json
{
  "status": "started"
}
```

또는 동기 방식인 경우:

```json
{
  "status": "completed",
  "scanned_files": 12451,
  "new_files": 32,
  "updated_files": 14,
  "inactive_files": 5
}
```

---

# FR-026 인덱싱 상태 확인

Endpoint:

```text
GET /api/admin/index/status
```

예:

```json
{
  "status": "completed",
  "last_started_at": "2026-08-24T10:00:00",
  "last_completed_at": "2026-08-24T10:02:31",
  "scanned_files": 12451
}
```

---

# FR-027 파일 크기 표시

Backend에서는 Byte 단위로 저장한다.

Frontend에서는 사람이 읽기 쉬운 단위로 변환한다.

예:

```text
28301221 bytes
```

↓

```text
27.0 MB
```

---

# FR-028 파일 경로 복사

검색 결과의 경로 옆에:

```text
경로 복사
```

버튼을 제공한다.

클릭 시:

```text
navigator.clipboard
```

기능을 이용해 전체 파일 경로를 복사한다.

완료 메시지:

```text
경로가 복사되었습니다.
```

---

# FR-029 파일 열기

환경 검증 후 적용한다.

목표:

검색 결과에서 파일을 클릭하면 해당 파일을 실행한다.

주의:

일반 웹 브라우저에서는 보안상 로컬 및 SMB 파일 실행이 제한될 수 있다.

따라서 MVP에서는 우선:

```text
경로 복사
```

기능을 필수로 적용하고

```text
파일 열기
폴더 열기
```

기능은 사내 운영환경을 확인한 후 별도 구현한다.

---

# FR-030 검색 UI

기본 화면 구성:

```text
┌──────────────────────────────────────┐
│ 사내 문서 검색                       │
│                                      │
│ [ 스타필드 제안서 찾아줘        ] 🔍 │
│                                      │
│ 검색 결과 12건                       │
│                                      │
│ PPTX                                 │
│ 스타필드 광고제안서 최종             │
│ \\NAS\영업\제안서\...                │
│ 수정: 2026.08.20                     │
│                              [복사]  │
│                                      │
│ PDF                                  │
│ 스타필드 운영계획                    │
│ ...                                  │
└──────────────────────────────────────┘
```

---

# FR-031 검색 진행 상태

API 요청 중:

```text
검색 중...
```

또는 Loading Indicator 표시

중복 검색 요청을 방지하기 위해 검색 버튼을 일시 비활성화할 수 있다.

---

# FR-032 Backend 오류

Backend API 호출 실패 시:

```text
검색 중 오류가 발생했습니다.
잠시 후 다시 시도해주세요.
```

개발 모드에서는 상세 오류를 console에 기록한다.

사용자 화면에는 Stack Trace를 표시하지 않는다.

---

# FR-033 파일서버 접근 오류

Indexing Service에서 다음 문제가 발생할 수 있다.

- NAS 연결 실패
- 접근 권한 없음
- 파일 삭제
- 파일 사용 중
- 경로 오류

전체 인덱싱을 중단하지 않고 해당 파일 또는 폴더만 건너뛴다.

오류는 로그에 기록한다.

예:

```text
2026-08-24 10:01:20
WARNING
Permission denied:
\\NAS\관리부\기밀자료
```

---

# FR-034 검색 로그

향후 검색 품질 개선을 위해 검색 내역을 저장할 수 있도록 구조를 준비한다.

search_logs:

```text
id
query
keywords
result_count
searched_at
```

선택:

```text
user_id
```

1차 MVP에서는 사용자 개인정보 없이 검색 Query만 저장할 수 있다.

---

# 7. DB 상세 명세

## 7.1 files

```text
files
```

| Column | Type | 조건 |
|---|---|---|
| id | INTEGER | PK |
| file_name | TEXT | NOT NULL |
| file_name_normalized | TEXT | NOT NULL |
| extension | TEXT | |
| full_path | TEXT | UNIQUE |
| folder_path | TEXT | |
| file_size | INTEGER | |
| created_at | DATETIME | |
| modified_at | DATETIME | |
| indexed_at | DATETIME | |
| is_active | BOOLEAN | DEFAULT TRUE |

---

## 7.2 index_status

| Column | Type |
|---|---|
| id | INTEGER |
| status | TEXT |
| started_at | DATETIME |
| completed_at | DATETIME |
| scanned_files | INTEGER |
| new_files | INTEGER |
| updated_files | INTEGER |
| inactive_files | INTEGER |
| error_count | INTEGER |

---

## 7.3 search_logs

| Column | Type |
|---|---|
| id | INTEGER |
| query | TEXT |
| keywords | TEXT |
| result_count | INTEGER |
| searched_at | DATETIME |

---

# 8. Backend 디렉터리 구조

권장 구조:

```text
backend/
│
├─ app/
│   ├─ main.py
│   │
│   ├─ api/
│   │   ├─ search.py
│   │   ├─ health.py
│   │   └─ admin.py
│   │
│   ├─ services/
│   │   ├─ search_service.py
│   │   ├─ index_service.py
│   │   └─ query_parser.py
│   │
│   ├─ database/
│   │   ├─ database.py
│   │   ├─ models.py
│   │   └─ repositories.py
│   │
│   ├─ schemas/
│   │   ├─ search.py
│   │   └─ index.py
│   │
│   └─ core/
│       ├─ config.py
│       └─ logging.py
│
├─ config/
│   ├─ stopwords.json
│   └─ extensions.json
│
├─ tests/
│
├─ requirements.txt
│
└─ README.md
```

---

# 9. Frontend 디렉터리 구조

```text
frontend/
│
├─ src/
│   │
│   ├─ components/
│   │   ├─ SearchBar.tsx
│   │   ├─ SearchResult.tsx
│   │   ├─ SearchResultList.tsx
│   │   └─ Loading.tsx
│   │
│   ├─ services/
│   │   └─ searchApi.ts
│   │
│   ├─ types/
│   │   └─ search.ts
│   │
│   ├─ pages/
│   │   └─ SearchPage.tsx
│   │
│   ├─ App.tsx
│   └─ main.tsx
│
└─ package.json
```

---

# 10. 검색 Backend 기본 로직

Pseudo Code:

```text
function search(query):

    query = normalize(query)

    keywords = extract_keywords(query)

    if keywords empty:
        return error

    files = database.search(keywords)

    results = []

    for file in files:

        score = calculate_score(
            file,
            keywords
        )

        if score > minimum_score:
            results.append(
                file,
                score
            )

    sort results by:
        score desc
        modified_at desc

    return results
```

---

# 11. 파일 인덱싱 기본 로직

```text
function index_files():

    start index log

    for root_path in configured_paths:

        for file in recursive_scan(root_path):

            try:

                metadata =
                    get_file_metadata(file)

                if database has file.full_path:

                    compare modified_at

                    if changed:
                        update

                else:
                    insert

            except PermissionError:

                log warning

            except Exception:

                log error

    mark missing files inactive

    complete index log
```

---

# 12. 설정 파일

## 환경변수

`.env`

예:

```text
DATABASE_URL=sqlite:///./files.db

INDEX_ROOT_PATH=\\NAS\공용자료

INDEX_INTERVAL_MINUTES=30

SEARCH_DEFAULT_LIMIT=20

SEARCH_MAX_LIMIT=100
```

실제 운영 시 경로와 환경값은 Source Code에 직접 작성하지 않는다.

---

# 13. 검색 성능 기준

MVP 목표:

파일:

```text
100,000개 이하
```

기준으로 일반 검색 응답:

```text
2초 이내
```

권장 목표:

```text
1초 이내
```

파일 수 증가 시:

- DB Index
- Full Text Search
- PostgreSQL
- Elasticsearch/OpenSearch

등의 적용을 검토한다.

---

# 14. DB Index 권장

다음 컬럼 Index 생성:

```text
file_name_normalized
extension
modified_at
is_active
```

`full_path`:

UNIQUE INDEX

---

# 15. 검색 결과 보안 원칙

시스템이 파일 경로를 반환한다고 해서 사용자에게 새로운 파일 접근권한을 부여해서는 안 된다.

원칙:

```text
기존 Windows/NAS 접근권한 유지
```

사용자가 파일에 접근 권한이 없다면 OS 또는 파일서버가 접근을 차단한다.

향후:

사용자 로그인 계정과 파일 권한을 연동할 수 있도록 구조를 확장한다.

---

# 16. 로그 정책

Backend 로그:

```text
INFO
WARNING
ERROR
```

예:

```text
INFO
Search query: 스타필드 제안서
Results: 12
Duration: 0.184s
```

Index 로그:

```text
INFO
Index completed

Scanned: 12,451
New: 32
Updated: 14
Inactive: 5
Errors: 2
```

---

# 17. 주요 오류 코드

## SEARCH_001

검색어 없음

HTTP:

```text
400
```

---

## SEARCH_002

검색 중 DB 오류

HTTP:

```text
500
```

---

## INDEX_001

검색 대상 경로 없음

```text
Configured directory not found
```

---

## INDEX_002

폴더 접근권한 없음

```text
Permission denied
```

---

## INDEX_003

DB 저장 실패

```text
Database write error
```

---

# 18. 주요 테스트 케이스

## TC-001

입력:

```text
스타필드
```

파일:

```text
스타필드_광고제안서.pptx
```

기대:

검색 결과 포함

---

## TC-002

입력:

```text
스타필드 제안서
```

파일:

```text
2026_스타필드_광고_제안서.pptx
```

기대:

높은 순위

---

## TC-003

입력:

```text
스타필드
```

파일명:

```text
최종제안서.pptx
```

경로:

```text
\\NAS\영업\스타필드\
```

기대:

검색 결과 포함

---

## TC-004

입력:

공백

기대:

검색 요청 차단

---

## TC-005

존재하지 않는 검색어:

```text
ZZZZZZZZ
```

기대:

```text
검색 결과 없음
```

---

## TC-006

사용자 입력:

```text
스타필드 PPT
```

기대:

PPT/PPTX 파일 우선 검색

---

## TC-007

같은 이름의 파일 존재

```text
제안서.pptx
```

여러 폴더에 존재

기대:

각각 별도 검색 결과로 표시

---

## TC-008

NAS 파일 삭제 후 재인덱싱

기대:

```text
is_active = false
```

---

## TC-009

새로운 파일 생성 후 재인덱싱

기대:

DB 신규 등록

---

## TC-010

파일 수정 후 재인덱싱

기대:

```text
modified_at
file_size
indexed_at
```

업데이트

---

# 19. 성능 테스트

테스트 데이터:

```text
1,000 파일
10,000 파일
50,000 파일
100,000 파일
```

각 단계에서 측정:

- 최초 인덱싱 시간
- 증분 인덱싱 시간
- 검색 응답 시간
- DB 크기
- CPU 사용량
- 메모리 사용량

---

# 20. 1차 MVP 개발 순서

## STEP 1

Backend 프로젝트 생성

- FastAPI
- SQLite
- 기본 설정

---

## STEP 2

파일 Scan 기능 구현

지정 폴더:

```text
D:\test_company_files
```

부터 테스트한다.

---

## STEP 3

DB 구현

files 테이블 생성

---

## STEP 4

Indexing Service 구현

파일 정보 DB 저장

---

## STEP 5

Search API 구현

```text
GET /api/search
```

---

## STEP 6

검색 점수 구현

파일명 + 폴더명 검색

---

## STEP 7

Swagger 테스트

```text
http://127.0.0.1:8000/docs
```

에서 API 테스트

---

## STEP 8

Frontend 생성

React + TypeScript + Vite

---

## STEP 9

검색 UI 구현

검색창

검색 결과 목록

---

## STEP 10

Frontend ↔ Backend 연결

---

## STEP 11

실제 사내 테스트 폴더 연결

---

## STEP 12

검색 품질 조정

- 불용어
- 검색 점수
- 최신 파일 가중치

---

# 21. MVP 완료 체크리스트

- [ ] FastAPI 서버 실행 가능
- [ ] SQLite DB 생성 가능
- [ ] 검색 대상 폴더 설정 가능
- [ ] 하위 폴더 재귀 탐색 가능
- [ ] 파일 메타데이터 수집 가능
- [ ] 파일 DB 저장 가능
- [ ] 신규 파일 탐지 가능
- [ ] 수정 파일 업데이트 가능
- [ ] 삭제 파일 비활성 처리 가능
- [ ] 검색어 입력 가능
- [ ] 검색어 전처리 가능
- [ ] 파일명 검색 가능
- [ ] 폴더명 검색 가능
- [ ] 복수 키워드 검색 가능
- [ ] 검색 점수 계산 가능
- [ ] 관련도 순 정렬 가능
- [ ] 수정일 기준 보조 정렬 가능
- [ ] 검색 결과 API 반환 가능
- [ ] Frontend 검색창 구현
- [ ] Frontend 결과 목록 구현
- [ ] 파일 경로 표시
- [ ] 파일 경로 복사
- [ ] 검색 결과 없음 처리
- [ ] Backend 오류 처리
- [ ] 파일 접근 오류 처리
- [ ] 인덱싱 로그 기록
- [ ] 검색 로그 기록
- [ ] 실제 사내 파일 테스트 완료

---

# 22. MVP 이후 확장 시 기존 구조 활용

## 2차

파일 내부 텍스트 추출

추가 DB:

```text
documents
document_contents
```

PDF / PPTX / DOCX / XLSX 텍스트 추출

---

## 3차

Vector DB 도입

문서 Chunk 생성

Embedding 생성

의미 기반 검색

---

## 4차

RAG

사용자:

```text
스타필드 제안서에서 광고 전략이 뭐였어?
```

시스템:

관련 문서 탐색

↓

관련 Chunk 검색

↓

LLM 전달

↓

답변 생성

---

## 5차

사내 업무 챗봇

파일검색 외 기능 추가

예:

```text
A파일 찾아줘

A파일 요약해줘

A파일과 B파일 비교해줘

작년 제안서에서 비슷한 사례 찾아줘

관련 제안서를 참고해서 초안 만들어줘
```

---

# 23. 개발 원칙

### 원칙 1

AI보다 검색 시스템을 먼저 완성한다.

### 원칙 2

파일 원본과 파일 메타데이터를 분리한다.

### 원칙 3

검색할 때 NAS 전체를 매번 탐색하지 않는다.

### 원칙 4

파일 검색은 DB 인덱스를 기준으로 수행한다.

### 원칙 5

검색 정확도 개선을 위해 검색 로직을 독립 Service로 구성한다.

### 원칙 6

향후 LLM/RAG를 붙이더라도 기존 파일검색 구조를 유지한다.

---

# 24. 최종 MVP 정의

본 프로젝트의 첫 번째 완성 상태는 다음 질문에 안정적으로 답할 수 있는 상태로 정의한다.

```text
사용자:
스타필드 제안서 찾아줘
```

시스템:

```text
관련 파일 3개를 찾았습니다.

1.
스타필드_광고제안서_최종.pptx

\\NAS\영업\제안서\2026\스타필드_광고제안서_최종.pptx

수정일
2026.08.20

2.
스타필드_운영제안서.pdf

\\NAS\운영\스타필드\스타필드_운영제안서.pdf

수정일
2026.07.13
```

즉 1차 MVP의 성공 기준은

> **사내 공유폴더에 있는 파일을 사용자가 파일 위치를 기억하지 못하더라도 키워드나 간단한 자연어 검색으로 찾아낼 수 있는 것**

으로 정의한다.